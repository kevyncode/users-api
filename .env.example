# Database Configuration for sd3db (CIS compatible)
# Copy this file to .env and update with your actual values

# MySQL Host (default: localhost)
DB_HOST=localhost

# MySQL Port (CIS uses 3307, not default 3306)
DB_PORT=3307

# Database Name (CIS schema name)
DB_NAME=sd3

# Database Username
DB_USERNAME=root

# Database Password (CIS default password)
DB_PASSWORD=your_password_here