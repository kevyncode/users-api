package jalau.usersapi.presentation.controllers;

import org.springframework.web.bind.annotation.RestController;
import org.springframework.web.bind.annotation.RequestMapping;

/**
 * REST Controller for User query operations
 * 
 * This controller handles all HTTP GET requests related to users.
 * It follows the Command Query Responsibility Segregation (CQRS) pattern.
 */
@RestController
@RequestMapping("/api/users")
public class UserQueryController {
    
    // TODO: GET verb related methods will be defined and implemented here in upcoming user stories
}
